TARGETNAME=i2cctrl_emu
TARGETTYPE=DRIVER
# Evaluates the build suffix appended by the WDK environment
!if "$(BUILD_ALT_DIR)" == "chk" || "$(BUILD_ALT_DIR)" == "_chk" || "$(FREEBUILD)" == "0"
TARGETPATH=..\bin-debug
!else
TARGETPATH=..\bin
!endif
INCLUDES=..\;.\include

MSC_WARNING_LEVEL=/W3 /WX

SOURCES= \
    i2cctrl_emu.c \
	i2cctrl_emu_acpi.c \
    i2cctrl_emu_ops.c \
    i2cctrl_emu_hid.c \
    i2cctrl_emu_ioctl.c \
    i2cctrl_emu_version.rc

TARGETLIBS= \
    $(DDK_LIB_PATH)\wdmsec.lib
